apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "swift-app.fullname" . }}-test-connection"
  labels:
    {{- include "swift-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "swift-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
