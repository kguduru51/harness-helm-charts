apiVersion: v1
kind: Service
metadata:
  name: {{ include "swift-app.fullname" . }}
  labels:
    {{- include "swift-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swift-app.selectorLabels" . | nindent 4 }}
